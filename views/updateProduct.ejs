<!DOCTYPE html>
<html>
<head><title>Update Product</title></head>
<body>
  <h2>Update Product</h2>
  <form action="/updateProduct/<%= product.id %>" method="POST" enctype="multipart/form-data">
    <div>
      <label>Product Name</label>
      <input type="text" name="ProductName" value="<%= product.ProductName %>" required>
    </div>
    <div>
      <label>Quantity</label>
      <input type="number" name="quantity" min="0" value="<%= product.quantity %>">
    </div>
    <div>
      <label>Price</label>
      <input type="number" step="0.01" name="price" value="<%= product.price %>">
    </div>
    <input type="hidden" name="currentImage" value="<%= product.image %>">
    <div>
      <label>Replace Image</label>
      <input type="file" name="image" accept="image/*">
      <% if (product.image) { %>
        <div>Current: <img src="/images/<%= product.image %>" style="max-height:80px;"></div>
      <% } %>
    </div>
    <button type="submit">Update</button>
  </form>
</body>
</html>